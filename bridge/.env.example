# Path to the PlusOne SQLite export (a copy — the bridge opens this READ-ONLY
# and never writes to it, never touches the PlusOne extension itself).
PLUSONE_DB_PATH=./data/plusone_backup.sqlite

PORT=5050
CLIENT_URL=http://localhost:5173
